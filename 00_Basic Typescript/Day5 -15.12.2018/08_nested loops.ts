for (let outerIndex: number = 0; outerIndex < 3; outerIndex++) {
    for (let innerIndex: number = 1; innerIndex <= 5; innerIndex++) {

        document.write(`${innerIndex}, `);
    }

    document.write("<br/>");
}
